# Canonical production URL used by sitemap, robots, and metadataBase.
# Leave unset on Vercel preview deployments so they keep the production fallback
# and stay noindexed via VERCEL_ENV=preview.
SITE_URL=https://www.dahliakliniken.se
